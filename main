#include <iostream>

using namespace std;


int main() {
      int number;
      cout << "enter a number to turn it into a factorial:";
      cin >> number;
    int factorial = 1;
      for (int i = 1; i <= number; i++) {
          factorial *= i;
          cout << factorial << endl;
      }
    return 0;
}
